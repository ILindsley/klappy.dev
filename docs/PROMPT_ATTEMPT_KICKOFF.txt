You are an implementation agent starting a NEW, INDEPENDENT ATTEMPT.

================================================================================
CRITICAL: READ THIS ENTIRE PROMPT. DO NOT STOP UNTIL ALL COMPLETION GATES PASS.
================================================================================

---

FIRST ACTION (before anything else): Register this attempt.

- If you have terminal access: run `npm run attempt:register -- --prd v0.2`
- If you do not: ask the human to run it and provide the resulting .attempt.json contents.

This creates your unique run_id and runs_dir. Evidence artifacts go ONLY in runs_dir.
Code changes live in the worktree repo as normal.

---

STEP 0: NUKE FIRST (independence).

If /src contains any prior app code, delete it before implementing.
Run: `npm run attempt:reset` OR manually delete /src contents.
This attempt must start from a blank app surface.
Do not build on existing app code.

---

Context:

- This repository follows Outcomes-Driven Development (ODD).
- A PRD is a hypothesis. An attempt is an observation.
- Attempts must be independent; do not reuse or steer from prior attempts.
- /src may be empty or nuked. You are choosing your stack from scratch.

---

Your task:

1. Read the active PRD:
   /docs/PRD.md

2. Read the deploy contract:
   /infra/contracts/build-output.md

3. Read required orientation documents:

   MUST READ:
   - /canon/constraints.md
   - /canon/definition-of-done.md

   REFERENCE IF NEEDED:
   - /canon/index.md
   - /canon/odd/manifesto.md

4. Choose your stack.

   You may use any framework: React, Svelte, Vue, Vanilla JS, Astro, etc.
   Or no framework at all.
   
   The only requirement: your build must output to /dist per the deploy contract.

5. Implement the plan from scratch.

   - Build everything fresh.
   - Do not look for existing components — there are none.
   - Prefer clarity and evidence over cleverness.

6. Continuously verify your work locally.

   Your build must be invokable via the repo's standard build command (`npm run build`)
   OR you must update repo scripts so `npm run build` produces /dist.
   
   Test navigation, UX, and failure cases relevant to the PRD.

7. FIRST SUBMIT (MANDATORY after first successful build):

   Once `npm run build` passes:
   
   npm run attempt:submit
   
   This commits and pushes your branch. Cloudflare will generate a preview URL.
   DO NOT PROCEED TO NEXT STEPS UNTIL THIS SUCCEEDS.

8. Write your evidence bundle to YOUR runs_dir:

   (Read .attempt.json for the exact path, e.g., attempts/prd-v0.2/_runs/a1b2c3d4/)

   Files to create:
     ATTEMPT.md      # what you built, approach taken, stack chosen
     EVIDENCE.md     # index of evidence files with descriptions
     evidence/       # screenshots proving the app works

   META.json is already created by attempt:register.

9. Capture visual evidence (REQUIRED):

   Use browser tools to take screenshots showing:
   - [ ] App loads correctly (desktop)
   - [ ] App loads correctly (mobile width)
   - [ ] At least one PRD feature working
   
   Save to: <runs_dir>/evidence/

10. FINAL SUBMIT (MANDATORY):

    npm run attempt:submit
    
    This pushes your final state including evidence.

---

Constraints:

- Write artifacts ONLY to your runs_dir (from .attempt.json)
- Do NOT write to attempt-001/, attempt-002/, etc. — those are assigned later
- Do not change the PRD during this attempt.
- Do not claim completion without evidence.
- Must output to /dist (see deploy contract)

---

Infra protection:

Do not change /infra/** (deploy contract) during an attempt unless the PRD explicitly requires it.
If you think the contract is wrong, propose it in PRD_PATCH.md in your runs_dir.

---

If you discover the PRD is flawed, incomplete, or contradictory:

- Stop.
- Report the issue clearly.
- Create a PRD_PATCH.md proposal in your runs_dir.
- Do not "fix" the PRD implicitly in code.

---

================================================================================
COMPLETION GATES — YOU ARE NOT DONE UNTIL ALL OF THESE ARE TRUE:
================================================================================

Before stopping, verify EVERY item:

[ ] 1. `npm run build` passes without errors
[ ] 2. `npm run attempt:submit` was run (branch is pushed)
[ ] 3. ATTEMPT.md exists in runs_dir with: summary, stack, approach
[ ] 4. EVIDENCE.md exists in runs_dir with: screenshot index
[ ] 5. evidence/ folder contains at least 2 screenshots
[ ] 6. Screenshots show the app actually working (not just code)
[ ] 7. Final `npm run attempt:submit` was run after evidence was written

If ANY gate fails, you are not done. Fix it before stopping.

================================================================================
DO NOT STOP. DO NOT CLAIM COMPLETION. UNTIL ALL 7 GATES PASS.
================================================================================

---

Begin by running attempt:register (or requesting registration), then summarize the PRD in your own words and propose your plan (including stack choice).
